问: 我们在数据库操作的时候，比如 dao 层中当遇到一个 sql.ErrNoRows 的时候，是否应该 Wrap 这个 error，抛给上层。为什么，应该怎么做请写出代码？
答: 1.可以在底层直接处理,然后给上层一个不成功的信息,这样在本层终止异常
    2.抛给上层,上层收到之后进行根本原因的判定,然后去封装不同的响应
    1的优点在于在底层直接处理,error没有扩散,尽早处理异常
    2的优点在于中心化处理error,可以将错误处理部分整理到一起,减少对业务逻辑的干扰性
    鉴于我没想出什么好的中心化的处理方式,所以建议底层处理掉